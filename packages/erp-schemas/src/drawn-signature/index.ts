/**
 * Drawn Signature schema plugin
 *
 * Real drawn signature embedding from FileStorageService at render time,
 * with fallback behaviour (blank/placeholder/error).
 */

export const drawnSignature = {};
